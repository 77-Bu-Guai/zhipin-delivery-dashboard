# AI 接口配置模板
# 复制本文件为 .env.local 并填入真实 key（.env.local 已被 gitignore 忽略，不会提交）
#
# 说明：网页端的 AI 助手现在支持「前端可视化配置」——
#       打开助手右上角的「接口设置」，即可在
#       [科大讯飞星火] / [本地 Ollama] / [agnes] / [自定义接口] 之间切换，
#       并直接填写 Base URL、API Key、Model，配置保存在浏览器 localStorage。
#       所以网页端一般不需要改这个文件。
#
# 本文件里的变量只在两种情况下用到：
#   1) 前端选了「科大讯飞星火」且 API Key 留空时，由 dev server 自动注入 SPARK_API_KEY；
#   2) scripts/ 下的 Node 批处理脚本（classify-jobs / classify-factors 等）。

# ── 科大讯飞星火 Spark（Lite 版永久免费）────────────────────────────
# 从 https://console.xfyun.cn 获取「个人访问令牌 / HTTP 服务鉴权 APIPassword」
SPARK_API_KEY=your-key-here
# 可选：覆盖默认模型（默认 lite 免费版；可选 generalv3.5 / 4.0Ultra 等，部分收费）
# VITE_SPARK_MODEL=lite

# ── agnes（旧后端，可选）────────────────────────────────────────────
# 前端「接口设置」里选 agnes 且 Key 留空时，会回退读这里
# AGNES_API_KEY=your-agnes-key

# ── 本地 Ollama（完全离线、零费用，无需 Key）────────────────────────
# 1. 安装 https://ollama.com  2. 终端执行：ollama pull qwen2.5:7b  3. ollama serve
# 4. 前端「接口设置」选「本地 Ollama」，地址 http://localhost:11434/v1，模型填 qwen2.5:7b
