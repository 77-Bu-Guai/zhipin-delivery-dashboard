# 回归清单 · BUG-001 / BUG-002 / BUG-003（阶段④修复验证）

> 由资深开发（吴八哥）在研发给出「修复说明」后填写，供测试/产品在阶段⑤做验收。
> 原则：不只验证「原问题修没修」，还要验证「修复过程有没有打挂别的功能、后续改动有没有回归」。
> 模板见 `../templates/回归验证清单模板.md`。

- 修复版本：本工作区 2026-08-01 状态（含 BUG-004 流式加载改造后的全量复测）
- 回归执行人：资深开发（吴八哥）
- 回归日期：2026-08-01
- 复测环境：Node 22（managed）/ Windows / `npm run build` + `npm run lint` + `npm run check` + `git hook run pre-commit`

---

## 总览

| Bug | 标题 | 原严重度/优先级 | 原问题验证 | 关联功能 | 边界 | 结论 | 可关闭 |
|-----|------|----------------|-----------|----------|------|------|--------|
| BUG-001 | lint 门禁未通过（37 errors） | S2 / P0 | ✅ 通过 | ✅ 无回归 | ✅ 通过 | 已修复 | 是 |
| BUG-002 | 主 JS chunk 896KB 超 500KB | S3 / P2 | ✅ 通过 | ✅ 无回归 | ✅ 通过 | 已修复 | 是 |
| BUG-003 | 评分致命阈值 >=1000 与 -5000 语义 | S3 / P3 | ✅ 通过 | ✅ 无回归 | ✅ 通过 | 已收口 | 是 |

> 注：三项修复后，又落地了 BUG-004（去 HTML 内嵌改流式加载），其改动涉及 `vite.config.ts`、`src/utils/dataLoader.ts`、`electron/main.cjs`、`electron/preload.cjs`、`src/vite-env.d.ts`，均在下方「边界场景」中做了回归复测，确认未打挂 001/002/003 的成果。

---

## 一、BUG-001 · lint 门禁未通过（37 errors）

### 原问题验证（必须 100% 通过）
| 序号 | 验证点（取自 Bug 复现步骤） | 预期 | 结果 | 备注 |
|------|----------------------------|------|------|------|
| R1 | `npm run lint` | 退出码 0，0 errors（warning 需说明） | 通过 | 0 errors / 4 warnings（非阻断） |

### 关联功能影响（改动文件牵扯到的模块）
| 序号 | 受影响功能 | 验证动作 | 结果 | 备注 |
|------|------------|----------|------|------|
| I1 | 9 个清理文件（AIChat / AIProviderSettings / Layout / RawScoreBreakdown / ExportPage / TodayPage / JobCategoryPage / DeductionsPage / CategoryDetailPage / DashboardPage / useAppStore / dataLoader / aiScoringParser） | 应用正常启动、各路由可访问、AI 配置/导出/详情页可用 | 通过 | 删除项均为未使用 import / any 精确化 / 死代码，无运行时依赖 |

### 边界场景覆盖
| 序号 | 边界 | 验证动作 | 结果 |
|------|------|----------|------|
| B1 | BUG-004 后续改动（dataLoader/vite.config/electron 等）是否重新引入 lint 错误 | `npm run lint` 全仓复跑 | 通过（0 errors） |
| B2 | 提交门禁是否仍生效且「换电脑不漏拦」 | husky pre-commit 跑 lint-staged+check+lint，`git hook run pre-commit` 退出 0 | 通过（见 husky 升级） |

### 回归结论
- [x] 原问题解决（lint 0 errors）
- [x] 关联功能无回归
- [x] 边界场景通过
- 遗留问题 / 风险点：4 个非阻断 warning（Layout/TodayPage exhaustive-deps、Pagination react-refresh），建议专项清理，不阻塞上线。
- 是否可关闭：**是**（仍待产品/测试阶段⑤最终验收）

---

## 二、BUG-002 · 主 JS chunk 896KB 超 500KB 警告

### 原问题验证（必须 100% 通过）
| 序号 | 验证点（取自 Bug 复现步骤） | 预期 | 结果 | 备注 |
|------|----------------------------|------|------|------|
| R1 | `npm run build` 主包体积 | 主 index.js < 500KB，无体积警告 | 通过 | 396KB（gzip 75KB） |

### 关联功能影响（改动文件牵扯到的模块）
| 序号 | 受影响功能 | 验证动作 | 结果 | 备注 |
|------|------------|----------|------|------|
| I1 | ExportPage / TodayPage 导出（xlsx 改动态 import） | 点击「导出」生成 xlsx 正常 | 通过 | 阶段① TC-702 / TC-801~804 浏览器实测已确认 |
| I2 | 按需 chunk 加载 | xlsx（430KB）仅在导出时加载，不进首屏 | 通过 | manualChunks 拆分 react-vendor / icons / xlsx |

### 边界场景覆盖
| 序号 | 边界 | 验证动作 | 结果 |
|------|------|----------|------|
| B1 | BUG-004 改动后主包体积是否回升 | 重新 build，index.js 仍 396KB，500KB 警告未重现 | 通过 |
| B2 | 大体积数据（10.9MB extension-data.json）加载 | 经 IPC/XHR 异步流式加载，不阻塞首屏 HTML（26KB） | 通过（见 BUG-004） |

### 回归结论
- [x] 原问题解决（主包 896KB → 396KB）
- [x] 关联功能无回归
- [x] 边界场景通过
- 遗留问题 / 风险点：安装包仍含 10.9MB 数据文件（作为异步数据源，必要；首屏已不阻塞）。
- 是否可关闭：**是**（仍待产品/测试阶段⑤最终验收）

---

## 三、BUG-003 · 评分致命阈值 >=1000 与 -5000 语义待对齐

### 原问题验证（必须 100% 通过）
| 序号 | 验证点（取自 Bug 复现步骤） | 预期 | 结果 | 备注 |
|------|----------------------------|------|------|------|
| R1 | 致命判定阈值（>=1000）与需求「-5000 致命」一致性 | 输出不变，功能正确 | 通过 | 仅收敛来源，数值不变 |

### 关联功能影响（改动文件牵扯到的模块）
| 序号 | 受影响功能 | 验证动作 | 结果 | 备注 |
|------|------------|----------|------|------|
| I1 | JobDetailPage 致命徽标 / 分档图 | 正常渲染，归类行为不变 | 通过 | 输入相同评分 → 相同 hasFatal/fatalCount/分档 |
| I2 | AIChat 红线指令值 | 提取为 `AI_RED_LINE_DEDUCTION` 常量，值不变 | 通过 | 与解析层 `FATAL_DEDUCTION_THRESHOLD=1000` 配对，杜绝两端漂移 |

### 边界场景覆盖
| 序号 | 边界 | 验证动作 | 结果 |
|------|------|----------|------|
| B1 | 跨版本分值差异（-9999 / -1000 / -5000） | 同一批次内解析口径一致（scoringConstants 单一事实来源） | 通过 |
| B2 | tsc / lint | `npm run check` + `npm run lint` 均 0 errors | 通过 |

### 回归结论
- [x] 原问题解决（阈值常量单一来源，语义已收口）
- [x] 关联功能无回归
- [x] 边界场景通过
- 遗留问题 / 风险点：按产品原则「跨版本分值有差异正常，同时间段一致即可」，不再强行统一到单一值。
- 是否可关闭：**是**（已收口，待阶段⑤验收）

---

## 四、阶段④整体结论

- 三项修复（001 lint 门禁 / 002 主包体积 / 003 评分阈值）**原问题全部解决，关联功能无回归，边界场景通过**。
- 后续 BUG-004 涉及共享文件（dataLoader / vite.config / electron）的改造，已在各自边界场景（B1）复测，确认未打挂上述三项成果。
- 质量门禁现状：`npm run check` 0 errors、`npm run lint` 0 errors、`npm run build` 通过（主包 396KB），husky pre-commit 全链路退出 0。
- 建议：三项均可关闭，进入阶段⑤测试验收，最终由产品 + 资深开发双签上线。
