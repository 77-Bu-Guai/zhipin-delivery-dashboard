# 质量复盘 · v2026-08-01（首屏流式化 + 工程门禁版）

- 复盘日期：2026-08-01
- 参与人：资深开发（吴八哥）牵头，产品/测试（戚锦豪）
- 本版 Bug 总数：4  已关闭：4（待阶段⑤最终双签）  遗留：0 阻断
- 目标：不追责，让下一版少踩同样的坑，并把结论**沉淀为强制规范**。

---

## 一、Bug 分布

| 模块 | S1 | S2 | S3 | S4 | 合计 |
|------|----|----|----|----|------|
| 工程化/门禁 | 0 | 1（BUG-001） | 0 | 0 | 1 |
| 构建/性能 | 0 | 1（BUG-004） | 1（BUG-002） | 0 | 2 |
| 数据/评分 | 0 | 0 | 1（BUG-003） | 0 | 1 |
| AI 接口配置 | 0 | 0 | 0 | 0 | 0 |
| 导出 | 0 | 0 | 0 | 0 | 0 |
| 插件 | 0 | 0 | 0 | 0 | 0 |
| **合计** | 0 | 2 | 2 | 0 | **4** |

> 观察：Bug 全部集中在「工程化 + 构建/性能」，业务功能层（AI 配置/导出/数据）本轮 0 Bug——说明业务逻辑相对稳，**短板在工程基建（门禁缺失、性能未把关）**。

## 二、问题类型 & 根因分类

| 类型 | 数量 | 典型根因 | 证据 |
|------|------|----------|------|
| 流程缺失 | 1 | 仓库无 commit 门禁，脏代码（37 lint errors）可直接合入 | BUG-001；`.git/hooks` 原本为空 |
| 研发逻辑/性能意识 | 2 | ① xlsx 全量进主包，未做代码分割（BUG-002）② 为规避 Electron `file://` CORS 用 embed hack 把 10.9MB 塞进 HTML，牺牲首屏（BUG-004） | BUG-002/004；vite embed-data 插件 |
| 需求歧义/口径漂移 | 1 | 致命阈值 `>=1000` 散落解析层、需求文档提 `-5000`、AIChat 又写死 `-1000`，三处口径无单一来源 | BUG-003 |
| 测试漏覆盖 | 0 | —— | —— |
| 兼容性 | 0（并入 BUG-004 研发逻辑，属已知 Electron 坑） | —— | —— |

## 三、修复效率

- 定位：4/4 均一次定位到根因（静态审计 + grep 全链路追踪，无「盲改」）。
- 修复：4/4 一轮回归通过，**0 次打回**。
- 亮点：BUG-004 修复时主动 grep 出**孤儿 re-embed 脚本**（`prepare-embed.cjs`/`post-build-embed.cjs`/`embed-data.js` 3.6MB 死文件），从根上堵死「改了又被脚本注回去」的隐性回归——这是「修一个点」升级为「关一条路径」的典型。

## 四、遗留风险

- Electron 打包 GUI 首屏未真机实测（沙箱无显示器）——源码/build 层已验证，打包层待阶段⑤条件 1 闭环。
- 4 个非阻断 lint warning（exhaustive-deps / react-refresh）——技术债，不影响运行。
- 安装包仍含 10.9MB `extension-data.json`（作异步数据源，必要；首屏已不阻塞）。
- P1 用例集（25 条）与 AI 直连回退（TC-902/904）尚未深度回归。

## 五、问题归因（四维度）

- **流程问题**：缺「提交即拦截」的自动化门禁 → 脏代码能进仓（BUG-001 的真正温床）。本版已用 husky 补上。
- **需求问题**：评分口径未固化为「单一事实来源」，需求/提示词/代码三处各写各的 → 漂移风险（BUG-003）。本版已收敛到 `scoringConstants.ts`。
- **研发问题**：性能意识不足——主包不分割、首屏塞大数据，都是「能跑但不 premium」的取巧（BUG-002/004）。
- **测试问题**：本轮无测试侧漏测；但首屏体积、主包体积这类**性能指标此前不在门禁里**，属「测试标准缺项」，本版已补入 Checklist。

## 六、下一版本改进建议

| 改进项 | 归属 | 落地动作 | 目标版本 |
|--------|------|----------|----------|
| 首屏/主包体积纳入门禁 | 研发 | 已写进 Checklist A（dist/index.html KB 级、主包 <500KB）；下版可加 CI 体积断言 | 下版 |
| Electron 打包首屏真机计时常态化 | 研发/测试 | 每次涉及数据加载改动，`npm run dist` 后真机计时 | 本版上线前起 |
| 清理 4 个 lint warning | 研发 | exhaustive-deps 补依赖 / Pagination 拆常量文件 | 下版 |
| P1 用例集深度回归 | 测试 | 排期跑 TC-103.. 等 25 条 + AI 直连回退 | 下版 |
| extension 自动投递纳入回归 | 测试 | 搭 boss-extension 扩展环境跑 TC-1201/1203 | 插件专项 |

## 七、规范沉淀（本版已落地）

- [x] 已更新 `研发质量Checklist.md`：
  - A 节新增「首屏 HTML KB 级 + 主包 <500KB」体积红线
  - A 节新增「husky pre-commit 随仓库分发，clone+install 即生效」
  - D 节新增「Electron 数据加载走 IPC 白名单读盘，禁 nodeIntegration，禁 HTML 内嵌大数据」
  - E 节强化「阈值统一走 `scoringConstants.ts` 单一事实源」
- [x] 已转为自动化门禁：husky 9 + lint-staged（`eslint --fix` → `tsc` → `eslint`，error 级拦截），真实 commit（f023c37）实战验证退出 0。
- [x] 已沉淀可复用工作流：本轮「六阶段 QA 生命周期」全流程走通（准备→反馈→定位→验证→验收→复盘），文档体系可作为后续版本模板。
- [ ] 测试用例库补充首屏/主包体积断言用例（转下版测试动作）。

---

## 一句话结论

**业务稳、基建弱**——4 个 Bug 全出在工程化与性能，全部一轮修复。本版最大收益不是修了 4 个 Bug，而是把「提交门禁 + 体积红线 + 阈值单一来源 + Electron 安全读盘」四条沉淀成**换人换电脑都拦得住**的强制规范。下一版把性能指标也塞进 CI，短板即补齐。
