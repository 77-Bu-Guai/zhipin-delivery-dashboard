# 工作区对话整理与自动化更新建议

## 一、近期对工作流有影响的变更

| 时间 | 变更 | 对阶段1影响 | 对阶段2影响 |
|------|------|------------|------------|
| 07-26 | 投递记录分类改为 5 类：success / aiRejected / addressRejected / basicRejected / pending | overview 统计字段需同步 | 误判/漏放分析维度需扩展 |
| 07-26 | 单休/大小周等致命项扣分从 1000 提升到 5000 | scoreDistribution 阈值需调整 | 硬性排除条款数值必须更新 |
| 07-29 | SQLite 成为唯一数据源，`extension-data.json` 仍由 DB 生成 | 输入文件路径仍可用，建议备注数据源 | 无直接影响 |
| 07-29 | 监控摘要新增「今日」维度 | 可在 JSON 中增加 today 字段 | 无直接影响 |
| 07-27 | 筛选原因列拆为 8 列同级展示 | 无 | 无 |
| 07-29 | `public/extension-data.json` 累计已 5399 条，tracker 停在 5275 | 下次运行会处理 100+ 条新增 | 无 |

## 二、阶段1「数据分析师」更新建议

1. **overview 统计改成 5 类**
   - `successCount`：status='success'
   - `aiRejectedCount`：status='warn' && processorType='aiFiltering'
   - `addressRejectedCount`：status='warn' && processorType='amap'
   - `basicRejectedCount`：status='warn' && 其他 processorType
   - `pendingCount`：status='pending' / 'failed'
   - 原 `aiFilteredCount` 改名为 `aiRejectedCount`
   - 原 `amapFilteredCount` 改名为 `addressRejectedCount`

2. **scoreDistribution 阈值调整**
   - 因为致命项已到 5000 分，建议新增 `fatal_ge1000` 档
   - 或把 `fatal_lowerThanNeg500` 改为 `fatal_lowerThanNeg1000`

3. **增加 today 维度**
   - 新增 `today` 字段：今日投递数、今日成功数、今日 AI 拒绝数
   - 与 Helper 的 `web-geek-job-Today` 对齐，方便阶段2看当日效果

4. **追踪文件逻辑**
   - 当前 `scripts/.analyze_tracker.json` 的 `last_analyzed_count=5275`
   - 与 DB 累计 5399 对比，下次会分析约 124 条新增
   - 若未来直接从 SQLite 读，可改为按 `createdAt` 切片而非按计数

## 三、阶段2「AI提示词工程专家」更新建议

1. **硬性排除数值更新**
   - 大小周/单休：`-1000` → `-5000`
   - CET-4/6、英语工作语言、实习岗位、非深圳：需核对当前提示词实际值
   - 这些值必须与提示词内一致，否则优化建议会失真

2. **输入/输出文件现状**
   - 当前 `C:\Users\86136\Desktop\提示词\` 下没有任何 `.txt` 或 `.json` 文件
   - 自动化会先报错「无输入文件」
   - 需要先放入 `提示词v2-优化版.txt`，自动化才能跑通

3. **诊断维度扩展**
   - 不只分析 `aiRejected`，也要看 `addressRejected` 和 `basicRejected`
   - 特别是「工作地点偏远」「薪资提成模式」等非 AI 过滤，可能反推提示词规则

4. **执行时间**
   - 当前 23:00 → 23:20，20 分钟间隔基本够用
   - 但 5000+ 记录全量分析时，建议把阶段2推迟到 23:30，留足余量

## 四、下一步

- 选项 A：先创建/更新桌面提示词文件，再同步改自动化 prompt
- 选项 B：直接让我修改两个自动化的 prompt，把上述变更写进去
